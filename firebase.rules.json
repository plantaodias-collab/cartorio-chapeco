{
  "rules": {
    "kanban": {
      ".read": "auth != null && auth.token.firebase.sign_in_provider == 'password'",
      "newCards": {
        "$cardId": {
          ".write": "auth != null && auth.token.firebase.sign_in_provider == 'password'",
          ".validate": "newData.hasChildren(['id', 'descricao', 'situacao', 'priority', 'aba', 'updatedAt', 'updatedBy', 'updatedByName']) && newData.child('id').isString() && newData.child('id').val() == $cardId && newData.child('descricao').isString() && newData.child('descricao').val().length <= 1000 && newData.child('situacao').isString() && newData.child('priority').isString() && newData.child('aba').isString() && newData.child('updatedBy').val() == auth.token.email && newData.child('updatedByName').isString() && newData.child('updatedByName').val().length <= 120"
        }
      },
      "overrides": {
        "$cardId": {
          ".write": "auth != null && auth.token.firebase.sign_in_provider == 'password'",
          ".validate": "newData.hasChildren(['id', 'situacao', 'priority', 'updatedAt', 'updatedBy', 'updatedByName']) && newData.child('id').isString() && newData.child('id').val() == $cardId && newData.child('situacao').isString() && newData.child('priority').isString() && newData.child('updatedBy').val() == auth.token.email && newData.child('updatedByName').isString() && newData.child('updatedByName').val().length <= 120"
        }
      },
      "audit": {
        "$entryId": {
          ".write": "auth != null && auth.token.firebase.sign_in_provider == 'password' && !data.exists()",
          ".validate": "newData.hasChildren(['id', 'type', 'savedAt', 'updatedBy', 'updatedByName']) && newData.child('id').isString() && newData.child('type').isString() && newData.child('savedAt').isString() && newData.child('updatedBy').val() == auth.token.email && newData.child('updatedByName').isString() && newData.child('updatedByName').val().length <= 120"
        }
      }
    }
  }
}
